#ifndef GLOBAL_H
#define GLOBAL_H



class Global
{
public:
    Global();

    static int pixelSize;      // 像素编辑窗口一个像素的大小
    static int scaleWidth;     // 标尺宽度
    static int scaleOffset;    // 标尺上0点起始位置的偏移
};

#endif // GLOBAL_H
